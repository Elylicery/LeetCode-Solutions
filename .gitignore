# 1. Node.js 依赖目录（可通过 npm install 重建，无需跟踪）
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 2. TypeScript 编译产物（.ts 编译后生成的 .js/.map 文件，无需跟踪源码）
*.js
*.js.map
*.d.ts
dist/
build/

# 3. 测试相关文件（自动下载的文本文件，可通过脚本重建）
pride-and-prejudice.txt
*.txt~

# 4. 编辑器/IDE 本地配置（用户自定义配置，不共享）
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# 5. 环境变量与敏感文件（避免泄露配置）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 6. 其他临时文件
tmp/
logs/
coverage/